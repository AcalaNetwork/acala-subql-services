{
  "name": "@acala-subql/app-karura",
  "version": "0.1.0-alpha.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "patch:types": "node ../../scripts/patch-acala-types.js",
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen",
    "gen:schema": "node ./scripts/generate-schema.js && yarn run codegen"
  },
  "homepage": "https://github.com/subquery/subql-starter",
  "repository": "github:subquery/subql-starter",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "qwer951123",
  "license": "Apache-2.0",
  "devDependencies": {
    "@polkadot/api": "^6",
    "@subql/cli": "latest",
    "@subql/types": "latest",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "@acala-subql/module-tokens": "^1.0.0"
  }
}
