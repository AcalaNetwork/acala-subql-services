import { SubstrateExtrinsic, SubstrateEvent } from "@subql/types";
import { TokenHandler } from "@acala-subql/module-tokens";

const tokens = new TokenHandler(api);

// handle transfer

// export const handleTransfer = 