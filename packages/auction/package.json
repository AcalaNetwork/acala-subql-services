{
  "name": "@acala-subql-services/auction",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "start": "./node_modules/.bin/subql-node -f . --timeout=512 --batch-size=10 --port=3123",
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "qwer951123",
  "license": "Apache-2.0",
  "devDependencies": {
    "@polkadot/api": "^7.1.1",
    "@subql/cli": "^0.20.0",
    "@subql/types": "^0.11.0",
    "typescript": "4.2.4"
  },
  "dependencies": {
    "@acala-network/sdk": "^4.0.2-20",
    "@acala-network/sdk-core": "^4.0.2-20",
    "@acala-network/subql-utils": "^0.1.17",
    "@subql/node": "^1.4.1"
  }
}
